;this just get's it all started :)
; reall stuff is in other files
(ns music.core
  (:gen-class))

;Unix
(use 'overtone.live)

;windows
;you need to have supercollider installed 
;(use 'overtone.core)
;(boot-external-server)

;Other dependencies used by this project
(use 'overtone.synth.stringed)
;(use 'overtone.inst.drum)
